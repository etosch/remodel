remodel
=======

691ST  Project 2 - make->remodel

Usage
----
After cloning, run `build.sh`. 	`build.sh` performs the following steps:

 This will create a REMODELFILE in the current directory that will be used for testing. The following executables must be accessible from the current directory: `ocamlopt`, `md5`, `g++`. Note that the `md5` call is used to perform the tests and is not used in the remodel file. 